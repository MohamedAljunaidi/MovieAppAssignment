package com.chainreaction.network.common

import com.chainreaction.network.BuildConfig

object NetworkConstants {

    const val TIMEOUT_IN_SECONDS = 30L
    const val BASE_URL = BuildConfig.BASE_URL
}